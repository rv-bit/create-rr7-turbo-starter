{
	"build": {
		"builder": "DOCKERFILE",
		"dockerfilePath": "apps/web/Dockerfile",
		"context": ".",
		"watchPatterns": ["apps/web/**", "packages/**", "turbo.json", "package.json"]
	},
	"deploy": {
		"numReplicas": 1,
		"restartPolicyType": "ON_FAILURE",
		"sleepApplication": false
	}
}
